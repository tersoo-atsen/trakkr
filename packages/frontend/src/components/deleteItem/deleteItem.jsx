import React from 'react';

import './deleteItem.scss';

const DeleteItem = () => {
  const text = 'Delete Item';
  return (
    <div>
      <p>{text}</p>
    </div>
  );
};

export default DeleteItem;
